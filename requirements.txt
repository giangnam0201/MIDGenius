# --- core DSP / numerics -------------------------------------------------
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12          # MP3/WAV/FLAC decoding via libsndfile
soxr>=0.3                # high quality resampling

# --- MIDI ----------------------------------------------------------------
mido>=1.3

# --- polyphonic transcription -------------------------------------------
# Only the bundled ONNX weights are used, so install without its TensorFlow
# and legacy resampy pins:  pip install --no-deps basic-pitch
basic-pitch>=0.4
onnxruntime>=1.16

# --- source separation ---------------------------------------------------
torch>=2.0
torchaudio>=2.0
demucs>=4.0              # optional: falls back to torchaudio HDemucs, then HPSS

# --- optional ------------------------------------------------------------
# av>=10                 # extra decoder fallback for exotic containers
